import 'package:flutter/material.dart';

class TabSuggestMovieWidget extends StatelessWidget {
  const TabSuggestMovieWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red,);
  }
}
